# Secrets & credentials — never commit these
.env
credentials.json
token.json

# Python virtual environment
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# Archives
*.zip

# macOS
.DS_Store

# Logs
logs/

# Claude workspace files
.claude/

# Editor
.vscode/
.idea/

# Client data — contains PII (email → company map), never commit
known_clients.json

# Reconciliation tool outputs — contain client emails/PII, never commit
# (produced by tools/*_sessions_full.py and tools/reconcile_diff.py)
crossbeam_sessions.csv
crossbeam_events.csv
reconcile_diff.csv
*_sessions.csv
